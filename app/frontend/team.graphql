query team {
  team {
    ...Team
    canModify
    members {
      id
      name
      image
      projects {
        id
        title
      }
    }
  }
}

fragment Team on Team {
  id
  title
  slug
  theme
  inviteKey
}

query team {
  team {
    ...Team
    members {
      id
      name
      image
      projects {
        id
        title
      }
    }
  }
}

fragment Team on Team {
  id
  title
  slug
  theme
  canModify
  inviteKey
}

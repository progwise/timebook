query workHours($from: Date!, $to: Date) {
  workHours(from: $from, to: $to) {
    ...WorkHour
  }
}

fragment WorkHour on WorkHour {
  id
  date
  comment
  duration
  user {
    id
    name
  }
  project {
    ...Project
  }
  task {
    ...Task
  }
}

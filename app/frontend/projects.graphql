query projectsWithTasks {
  projects {
    ...ProjectWithTasks
  }
}

fragment Project on Project {
  id
  title
  startDate
  endDate
  canModify
}

fragment ProjectWithTasks on Project {
  id
  title
  startDate
  endDate
  canModify
  tasks {
    ...Task
  }
}

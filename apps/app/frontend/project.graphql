query project($projectId: ID!) {
  project(projectId: $projectId) {
    ...Project
    canModify
    tasks {
      canModify
      ...Task
    }
  }
}

fragment Task on Task {
  id
  title
  hasWorkHours
  project {
    id
    title
  }
}
